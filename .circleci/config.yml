version: 2

jobs:
  build:
    docker:
      - image: node:current
        environment:
          POSTGRES_TEST_URL: postgres://postgres@localhost:5432/postgres
      - image: postgres:latest
    steps:
      - checkout
      - run: yarn install
      - run: yarn test
      - store_test_results:
          path: reports
      - store_artifacts:
          path: reports
